{
  "name": "bun_spa",
  "private": true,
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "dev": "bun run build && bun run --parallel dev:server dev:watch",
    "dev:server": "bun --watch index.ts",
    "dev:watch": "bun build ./src/main.ts --outdir ./dist --watch --minify",
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/ && bun build ./src/main.ts --outdir ./dist --minify",
    "lint": "oxlint .",
    "fix": "oxlint --fix .",
    "preview": "NODE_ENV=production bun index.ts"
  },
  "dependencies": {
    "alpinejs": "^3.15.8",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxlint": "^1.47.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
